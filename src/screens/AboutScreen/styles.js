import { StyleSheet } from 'react-native';
import { GREY } from '@lusiaves/constants/colors';

export default StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
  },
  title: {
    marginTop: '20%',
    marginBottom: '10%',
    color: GREY,
    fontSize: 20,
  },
});
